<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.SubscriptionMapper">
    <insert id="insertSubscription">
        insert into UserDrugSubscription (openId,
                                          userName,
                                          createTime,
                                          drugUuid,
                                          subscriptionStatus)
        values (#{openId},
                #{userName},
                #{createTime},
                #{drugUuid},
                "0");
    </insert>

    <delete id="deleteSubscription">
        delete
        from UserDrugSubscription
        where openId = #{openId}
          and uuid = #{uuid}
    </delete>

    <update id="updateSubscription">
        update UserDrugSubscription
        set subscriptionStatus = #{status}
        where openId = #{openId}
          and drugUuid = #{drugUuid}
    </update>

</mapper>